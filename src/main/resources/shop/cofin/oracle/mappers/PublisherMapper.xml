<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.cofin.oracle.mappers.PublisherMapper">
	<insert id="save" parameterType="shop.cofin.oracle.publisher.domain.PublisherDto">
		insert into
		publishers (pub_id, pub_name, book_id, mar_name, phone)
		values (${pubId}, #{pubName}, #{marName}, #{phone})
	</insert>
</mapper>